node_modules/
dist/
coverage/
.env
.env.*
!.env.example
.secrets/
*.log
# Throwaway PostgreSQL clusters and other scratch data used by local validation runs.
# These are real database directories; they must never enter version control.
.tmp-*/
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# Live Microsoft/Xbox auth tokens for the bot account. Committing these is account takeover.
.minecraft-auth/

# AI assistant tooling. Not part of the product, and 730 files of it buries the code that is.
# Kept on disk, out of the repository.
.claude/
.agent/
.gemini/
skills-lock.json
